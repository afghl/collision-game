var VectorRotate = (function() {
  return {
    rotate : function(v, object2) {
      return {
        object1velocity: [-2, 0],
        object2velocity: [-22, 0]
      }
    }
  };

})();

module.exports = VectorRotate;
